-- NOTE:
-- This migration was introduced as a passport-only baseline snapshot and
-- duplicated objects already created by earlier migrations.
--
-- To keep existing environments upgradable, this migration is intentionally
-- a no-op. Structural changes for later commits should be added as dedicated
-- forward migrations.

-- no-op
SELECT 1;
